CREATE TABLE IF NOT EXISTS products (
    product_id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT NOT NULL,
    price DECIMAL(10, 2) NOT NULL CHECK (price > 0),
    stock_quantity INTEGER NOT NULL CHECK (stock_quantity >= 0),
    material VARCHAR(50) NOT NULL
);

CREATE INDEX idx_products_name ON products(name);
CREATE INDEX idx_products_material ON products(material);

INSERT INTO products (name, description, price, stock_quantity, material) VALUES
    ('Gold Ring', '14K Gold Ring with Diamond', 999.99, 10, 'Gold'),
    ('Silver Necklace', 'Sterling Silver Necklace', 299.99, 15, 'Silver'),
    ('Pearl Earrings', 'Natural Pearl Earrings', 199.99, 20, 'Pearl');